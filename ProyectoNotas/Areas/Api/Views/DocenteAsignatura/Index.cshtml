@model IEnumerable<ProyectoNotas.Areas.Api.Models.DocenteAsignatura>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Docentes
        </th>
        <th>
            Asignaturas
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Docentes.Nombres)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Asignaturas.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DocenteAsignaturaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DocenteAsignaturaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DocenteAsignaturaId })
        </td>
    </tr>
}

</table>
